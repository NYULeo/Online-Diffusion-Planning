# ODP (JAX/Flax port) — Python dependencies. No PyTorch: the port runs entirely on JAX.
#
# One-shot install (inside a conda/venv env):  pip install -r requirements.txt
#
# The jax line below pulls the CUDA 12 GPU build so a single `pip install -r` works on a CUDA server.
#   * CPU only / macOS: replace `jax[cuda12]>=0.4.26` with `jax[cpu]>=0.4.26`
#   * CUDA 11 server:    replace with `jax[cuda11_pip]>=0.4.26`

# --- core JAX stack ---
jax[cuda12]>=0.4.26
flax>=0.8.4
optax
distrax>=0.1.5
einops

# --- numerics / logging / plotting ---
numpy<2.0
scipy
sympy
wandb
tqdm
matplotlib
seaborn
mediapy

# --- environments / datasets ---
gymnasium<1.0.0
gymnasium-robotics>=1.4.0
ogbench
minari
h5py>=3.0.0
